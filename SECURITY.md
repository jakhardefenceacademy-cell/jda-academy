# JDA Security
- Never enter a Gmail/Google account password into the JDA app.
- JDA Admin Password is a local app login credential; only its SHA-256 hash is stored in this browser.
- Google Drive authorization must use the configured backend/Google authorization flow.
- Never publish cloud sync tokens in public source files.
- Use HTTPS on the custom domain.
