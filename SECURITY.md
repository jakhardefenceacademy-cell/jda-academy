# JDA Security

- Do not store Gmail passwords, Google OAuth tokens, or private keys in the website repository.
- Cloud sync uses a token stored in Google Apps Script Script Properties.
- Do not publish the sync token in HTML/JavaScript.
- Keep student documents/photos private where possible; GitHub Pages itself is public.
- Rotate the sync token immediately if it is exposed.
- GitHub Pages HTTPS should remain enabled.
